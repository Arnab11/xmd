<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="@color/ff_bg">

    <!-- Summary bar: "2 downloading  •  3 done  •  1 failed" -->
    <TextView
        android:id="@+id/queueSummary"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:paddingHorizontal="16dp"
        android:paddingVertical="10dp"
        android:textColor="@color/ff_muted"
        android:textSize="12sp"
        android:background="@color/ff_surface"
        android:visibility="gone" />

    <!-- Main content area: recycler OR empty label -->
    <FrameLayout
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1">

        <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/queueRecycler"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:paddingHorizontal="12dp"
            android:paddingTop="8dp"
            android:paddingBottom="8dp"
            android:clipToPadding="false" />

        <LinearLayout
            android:id="@+id/emptyContainer"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:orientation="vertical"
            android:gravity="center"
            android:visibility="gone">

            <ImageView
                android:layout_width="56dp"
                android:layout_height="56dp"
                android:src="@drawable/ic_downloads"
                android:tint="@color/ff_divider"
                android:contentDescription="@null"
                android:layout_marginBottom="16dp" />

            <TextView
                android:id="@+id/emptyLabel"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="No downloads yet"
                android:textColor="@color/ff_muted"
                android:textSize="15sp" />

            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginTop="6dp"
                android:text="Add links in the Home tab"
                android:textColor="@color/ff_muted"
                android:textSize="12sp"
                android:alpha="0.6" />
        </LinearLayout>
    </FrameLayout>

    <!-- Cancel All (only shown when queue has items) -->
    <com.google.android.material.button.MaterialButton
        android:id="@+id/cancelButton"
        style="@style/Widget.MaterialComponents.Button.OutlinedButton"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_margin="12dp"
        android:text="@string/action_cancel_all"
        app:strokeColor="@color/ff_error"
        android:textColor="@color/ff_error"
        android:visibility="gone" />

</LinearLayout>
